using System;

namespace MyApp // Note: actual namespace depends on the project name.
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int ageOfViewer = 12;
            if(ageOfViewer >= 12)
            {
                System.Console.WriteLine("The viewer is 12 or over.");
            }
        }
    }
}
